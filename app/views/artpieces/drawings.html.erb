<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <title>Drawings</title>

    <style media="screen">
      .carousel-inner {
        background-color: white;
      }

      .carousel-inner > .item > img,
      .carousel-inner > .item > a > img{
      width: 50%;
      max-height: inherit;
      margin: auto;

      }

      .drawing_title {
        margin: auto;
        width: 50%;
        text-align: center;
      }

    </style>
  </head>

  <body>

    <div class="col-lg-4 col-md-4 col-s-12 col-xs-12">
      <% @drawings.each do |drawing| %>
        <div class="item <%= 'active' if drawing == @drawings[0] %>">
          <%= image_tag drawing.image.url(:large), class: 'img-responsive' %>
          <p class="drawing_title"><%= drawing.title %></p>
        </div>
      <% end %>
    </div>

    <!-- <div id="drawingsCarousel" class="carousel slide" data-wrap="false" >

      <div class="carousel-inner" role="listbox">
        <% @drawings.each do |drawing| %>
          <div class="item <%= 'active' if drawing == @drawings[0] %>">
            <%= image_tag drawing.image.url(:medium), class: 'img-responsive' %>
            <p class="drawing_title"><%= drawing.title %></p>
          </div>
        <% end %>
      </div>

      <a class="left carousel-control" href="#drawingsCarousel" data-slide="prev">
        <span class="glyphicon glyphicon-chevron-left"></span>
      </a>
      <a class="right carousel-control" href="#drawingsCarousel" data-slide="next">
        <span class="glyphicon glyphicon-chevron-right"></span>
      </a>
    </div> -->


    <script src="https://ajax.googleapis.com/ajax/libs/jquery/1.12.4/jquery.min.js"></script>
    <script src="js/bootstrap.min.js"></script>
  </body>
</html>
